name: Deploy master
on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v2
      
    - name: Upload JAR
      uses: actions/upload-artifact@v2
      with:
        name: artifact
        path: src\CombatAnalysis.WebApp
    
    - name: Download JAR
      uses: actions/download-artifact@v2
      with:
        name: artifact
          
    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v20
      with:
        aws_access_key: AKIAVGQFZJBSR3RRZXMU
        aws_secret_key: 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
        application_name: details-analysis
        environment_name: Detailsanalysis-env
        version_label: 163
        region: us-east-1
        deployment_package: artifact
