name: Deploy master
on:
  pull_request:
    branches: [ "main" ]

env: 
  CI: false
  
jobs:
  build:
    runs-on: windows-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v2
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 3.1.x
      
    - name: Publish
      run: dotnet publish src\CombatAnalysis.WebApp\ /p:WebPublishMethod=Package /p:PackageLocation="publish"
      
    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v20
      with:
        aws_access_key: AKIAVGQFZJBSZQAR6F7E
        aws_secret_key: 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
        application_name: details-anslysis
        environment_name: Detailsanslysis-env
        version_label: 0.0.12
        region: us-east-1
        deployment_package: src\CombatAnalysis.WebApp\publish\CombatAnalysis.WebApp.zip
