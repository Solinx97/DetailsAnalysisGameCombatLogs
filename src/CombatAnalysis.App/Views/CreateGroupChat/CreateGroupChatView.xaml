<views:MvxWpfView
    x:Class="CombatAnalysis.App.Views.CreateGroupChat.CreateGroupChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:converters="clr-namespace:CombatAnalysis.App.Converters"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="0.25*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Grid.Row="0" Text="Создание группового чата" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="DemiBold"/>
        
        <StackPanel Grid.Column="1" Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Название чата" VerticalAlignment="Center" Margin="10"/>
                <TextBox VerticalAlignment="Center" Style="{StaticResource messageTextBox}" Text="{Binding Name}" Padding="5" VerticalContentAlignment="Center" Height="45" Width="200"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                <TextBlock Text="Приватность чата" VerticalAlignment="Center" Margin="10"/>
                <RadioButton Margin="50 5" Content="Открытый чат" FontSize="12" IsChecked="True" mvx:Bi.nd="Command GetPolicyTypeCommand, CommandParameter=0"/>
                <RadioButton Margin="50 5" Content="Закрытый чат" FontSize="12" mvx:Bi.nd="Command GetPolicyTypeCommand, CommandParameter=1"/>
                <RadioButton Margin="50 5" Content="Закрытый чат (переход по ссылке)" FontSize="12" mvx:Bi.nd="Command GetPolicyTypeCommand, CommandParameter=2"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" IsEnabled="False">
                <TextBlock Text="Ограничения чата" VerticalAlignment="Center" Margin="10"/>
                <CheckBox Margin="50 5" Content="Без ограничений" FontSize="12" IsChecked="True" mvx:Bi.nd="Command GetChatTypeCommand, CommandParameter=0"/>
                <CheckBox Margin="50 5" Content="Запрет на добавление новых пользователей" FontSize="12" mvx:Bi.nd="Command GetChatTypeCommand, CommandParameter=1"/>
                <CheckBox Margin="50 5" Content="Запрет на исключение пользователей из чата" FontSize="12" mvx:Bi.nd="Command GetChatTypeCommand, CommandParameter=2"/>
                <CheckBox Margin="50 5" Content="Ограничение на публикацию контента" FontSize="12" mvx:Bi.nd="Command GetChatTypeCommand, CommandParameter=3"/>
                <CheckBox Margin="50 5" Content="Только для чтения" FontSize="12" mvx:Bi.nd="Command GetChatTypeCommand, CommandParameter=4"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal">
            <Button mvx:Bi.nd="Command CreateCommand" Content="Создать" Width="100" Height="50" FontSize="16"
                    Margin="0" Style="{StaticResource showButton}" />
            <Button mvx:Bi.nd="Command AddUsersCommand" Content="Добавить пользователей" Width="225" Height="50" FontSize="16"
                    Margin="0" Style="{StaticResource showButton}" IsEnabled="False" />
            <Button mvx:Bi.nd="Command CancelCommand" Content="Отмена" Width="100" Height="50" FontSize="16"
                    Margin="0" Style="{StaticResource showButton}" />
        </StackPanel>
    </Grid>
</views:MvxWpfView>
