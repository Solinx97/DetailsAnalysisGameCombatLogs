<UserControl x:Class="CombatAnalysis.App.UserControls.ChatSeparator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CombatAnalysis.App.UserControls"
             mc:Ignorable="d" 
             d:DesignWidth="200" Name="chatSeparator" d:Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Separator Grid.Column="0" Background="#dadada" Height="0.8"/>
        <TextBlock Grid.Column="1" Margin="13 0" Foreground="#b7b7b7" FontSize="11" Text="{Binding ElementName=chatSeparator, Path=Title}" />
        <Separator Grid.Column="2" Background="#dadada" Height="0.8"/>
    </Grid>
</UserControl>
