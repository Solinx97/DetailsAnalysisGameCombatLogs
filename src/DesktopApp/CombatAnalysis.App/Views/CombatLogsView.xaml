<views:MvxWpfView
    x:Class="CombatAnalysis.App.Views.CombatLogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:CombatAnalysis.App.Converters"
    xmlns:loc="clr-namespace:CombatAnalysis.App.Localizations"
    xmlns:resx="clr-namespace:CombatAnalysis.App.Localizations.Resources.CombatLogs"
    xmlns:local="clr-namespace:CombatAnalysis.App.Views.ViewTemplates"
    xmlns:basicViewModel="clr-namespace:CombatAnalysis.Core.ViewModels.ViewModelTemplates;assembly=CombatAnalysis.Core"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:combatLogsViews="clr-namespace:CombatAnalysis.App.Views.CombatLogs"
    loc:LocalizationObject.ResourceManager="{x:Static resx:Resource.ResourceManager}"
    mc:Ignorable="d">

    <Grid>
        <Grid.Resources>
            <converters:TheNativeBoolToXAMLBoolConverter x:Key="BoolToXAMLBool" />
            <converters:TheNativeBoolToVisibilityConverter x:Key="BoolToVisibility" />
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="55"/>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <ContentControl Grid.RowSpan="2" Grid.ColumnSpan="3" Content="{Binding Basic}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type basicViewModel:BasicTemplateViewModel}">
                    <local:BasicTemplateView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <TextBlock Name="combatLogInformTitle" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{loc:Loc Title}" FontSize="18" Foreground="White" Margin="0 30" Style="{StaticResource titleText}"/>

        <TabControl Grid.Row="1" Grid.Column="2" Background="Transparent" BorderThickness="0" SelectedIndex="{Binding SelectedTabIndex}">
            <TabControl.Resources>
                <Style TargetType="TabPanel">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="{loc:Loc PublicLogs}" Style="{StaticResource TabItemStyle1}"
                     IsEnabled="{Binding IsAllowSwitchTabs, Converter={StaticResource BoolToXAMLBool}}">
                <combatLogsViews:PublicCombatLogsView DataContext="{Binding PublicCombatLogsVM}" />
            </TabItem>
            <TabItem Header="{loc:Loc PersonalLogs}" Style="{StaticResource TabItemStyle1}" 
                     Visibility="{Binding IsAuth, Converter={StaticResource BoolToVisibility}, ConverterParameter=true}"
                     IsEnabled="{Binding IsAllowSwitchTabs, Converter={StaticResource BoolToXAMLBool}}">
                <combatLogsViews:PrivateCombatLogsView DataContext="{Binding PrivateCombatLogsVM}" />
            </TabItem>
        </TabControl>
    </Grid>
</views:MvxWpfView>
