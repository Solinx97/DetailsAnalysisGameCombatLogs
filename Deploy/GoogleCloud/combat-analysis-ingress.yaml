apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: host-ingress-dev
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: user-api
            port:
              number: 80