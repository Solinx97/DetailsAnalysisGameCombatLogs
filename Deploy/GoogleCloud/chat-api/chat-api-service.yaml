apiVersion: v1
kind: Service
metadata:
  name: chat-api
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: chat-api
  ports:
  - port: 80
    targetPort: 80