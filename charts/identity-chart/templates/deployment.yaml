apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Values.container.name }}
          image: {{ .Values.container.image }}:{{ .Values.imageTags.identity.identity }}
          ports:
            - containerPort: {{ .Values.container.port }}
          env:
           - name: ASPNETCORE_ENVIRONMENT
             value: {{ .Values.env }}
           - name: ConnectionStrings_DefaultConnection
             value: "Server=host.docker.internal,1433;Database=Combat_Analysis.Identity;User Id=sa;Password=Oleg123;Integrated Security=False;Connect Timeout=120;Encrypt=True;Trust Server Certificate=True;Application Intent=ReadWrite;Multi Subnet Failover=False"
           - name: ConnectionStrings_UserConnection
             value: "Server=host.docker.internal,1433;Database=Combat_Analysis.User;User Id=sa;Password=Oleg123;Integrated Security=False;Connect Timeout=120;Encrypt=True;Trust Server Certificate=True;Application Intent=ReadWrite;Multi Subnet Failover=False"
           - name: API_User
             value: "https://api.user.analysis.com/"
           - name: API_Identity
             value: "https://identity.analysis.com/"
           - name: Authentication_GrantType_Code
             value: "code"
           - name: Authentication_GrantType_Authorization
             value: "authorizationCode"
           - name: Authentication_GrantType_RefreshToken
             value: "refreshToken"
           - name: Authentication_IssuerSigningKey
             value: "aFo/P+0M8TRKhjkIhjR1+1Y/b77eSl9Ewdj2e3hSxRQ="
           - name: Authentication_Issuer
             value: "https://identity.analysis.com/"
           - name: Authentication_Protocol
             value: "https"
           - name: Authentication_AccessTokenExpiresMins
             value: "60"
           - name: Authentication_RefreshTokenExpiresDays
             value: "7"
           - name: Certificate_PfxPath
             value: "/etc/certs/certificate.pfx"
           - name: Certificate_PWD
             valueFrom:
               secretKeyRef:
                 name: certificate-secret
                 key: password
          volumeMounts:
            - name: cert-volume
              mountPath: /etc/certs
              readOnly: true
      volumes:
        - name: cert-volume
          secret:
            secretName: certificate-secret