apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.user.name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.user.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Values.user.name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.user.name }}
    spec:
      containers:
        - name: {{ .Values.user.container.name }}
          image: {{ .Values.user.container.image }}:{{ .Values.imageTags.user }}
          ports:
            - containerPort: {{ .Values.user.container.port }}