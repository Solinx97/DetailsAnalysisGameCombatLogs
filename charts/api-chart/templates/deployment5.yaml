apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.app5.name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.app5.autoscaling.enabled }}
  replicas: {{ .Values.app5.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Values.app5.name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.app5.name }}
    spec:
      containers:
        - name: {{ .Values.app5.container.name }}
          image: {{ .Values.app5.container.image }}:{{ .Values.app5.container.tag }}
          ports:
            - containerPort: {{ .Values.app5.container.port }}