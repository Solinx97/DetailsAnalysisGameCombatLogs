apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.app5.name }}-service
  labels:
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-{{ .Values.app5.name }}
  ports:
    - name: http
      protocol: {{ .Values.service.protocol }}
      port: {{ .Values.service.port }}
      targetPort:  {{ .Values.service.targetPort }}
      nodePort: {{ .Values.app5.service.nodePort }}