apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.health.name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.health.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Values.health.name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.health.name }}
    spec:
      containers:
        - name: {{ .Values.health.container.name }}
          image: {{ .Values.health.container.image }}:{{ .Values.imageTags.health }}
          ports:
            - containerPort: {{ .Values.health.container.port }}