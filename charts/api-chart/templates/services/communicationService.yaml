apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.communication.name }}-service
  labels:
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-{{ .Values.communication.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.communication.service.port }}
      targetPort:  {{ .Values.communication.service.targetPort }}