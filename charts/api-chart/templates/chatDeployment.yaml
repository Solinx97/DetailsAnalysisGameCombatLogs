apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.chat.name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.chat.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Values.chat.name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.chat.name }}
    spec:
      containers:
        - name: {{ .Values.chat.container.name }}
          image: {{ .Values.chat.container.image }}:{{ .Values.imageTags.chat }}
          ports:
            - containerPort: {{ .Values.chat.container.port }}