apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.webApp.name }}-deployment
  labels:
    app: {{ .Release.Name }}
    env: {{ .Values.env }}
    owner: OlegFedosov
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Values.webApp.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.webApp.name }}
    spec:
      containers:
        - name: {{ .Values.webApp.container.name }}
          image: {{ .Values.webApp.container.image }}:{{ .Values.imageTags.webApp }}
          ports:
            - containerPort: {{ .Values.webApp.container.port }}